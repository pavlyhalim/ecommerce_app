import 'package:flutter/material.dart';

class PageWithIcon extends StatelessWidget {
  const PageWithIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
