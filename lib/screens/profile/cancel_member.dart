import 'package:flutter/material.dart';

class CancelMember extends StatelessWidget {
  const CancelMember({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
